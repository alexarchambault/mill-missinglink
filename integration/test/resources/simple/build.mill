package build

import mill._
import mill.scalalib._
import io.github.alexarchambault.millmissinglink._

/** A minimal project with no classpath conflicts. */
object foo extends ScalaModule with MissingLink {
  def scalaVersion = "2.13.16"
}
